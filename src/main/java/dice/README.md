# Домашнее задание: Цифровой кубик для "Днд'шника" 🎲 ## **Описание Задачи** Это домашнее задание поможет вам попрактиковаться в использовании случайных чисел и условных операторов `if-else` в Java. Вам предстоит создать консольное приложение, которое будет симулировать бросок **одного** из различных типов игральных кубиков, используемых в настольных ролевых играх (D&D). Программа должна позволять пользователю выбрать тип кубика (например, d4, d6, d8, d10, d12, d20) и затем выводить результат одного броска. * **Основная функциональность** * Программа должна запрашивать у пользователя, какой **тип кубика** он хочет бросить ( например, `d4`, `d6`, `d8`, `d10`, `d12`, `d20`). * После выбора типа, должен быть сгенерирован случайный результат **одного** броска в соответствии с выбранным типом (например, для d20 - от 1 до 20). * Результат броска должен быть выведен на экран. * **Дополнительные условия / Ограничения** * Обработка некорректного ввода пользователя (например, ввод текста вместо числа или выбор несуществующего типа кубика) должна быть реализована с помощью **`if-else if`** или **`switch`** операторов. * **Примеры использования** * **Пример сценария 1:** 1. Пользователь запускает приложение. 2. Приложение запрашивает: "Какой кубик бросить? (d4, d6, d8, d10, d12, d20):" 3. Пользователь вводит `d20`. 4. Приложение выводит: ``` Вы бросили d20. Результат: 17 ``` * **Пример сценария 2 (некорректный ввод):** 1. Пользователь запускает приложение. 2. Приложение запрашивает: "Какой кубик бросить? (d4, d6, d8, d10, d12, d20):" 3. Пользователь вводит `d9`. 4. Приложение выводит: "Некорректный тип кубика. Пожалуйста, выберите из d4, d6, d8, d10, d12, d20." ----- ## Входные / Выходные данные **Входные данные:** * Строка, представляющая тип кубика (например, `d4`, `d6`, `d20`). **Выходные данные:** * "Вы бросили [Тип Кубика]. Результат: X", где X — результат броска. * Сообщение об ошибке для некорректного ввода. ----- ## Технические требования * Для генерации случайных чисел используйте класс `Random` или `SecureRandom`. * Приложение должно работать в консоли. ----- ## **Бонусные Задания (Необязательно)** * **Модификатор броска:** * После броска кубика, если результат, например, 20 (для d20) или 1, можно вывести дополнительное сообщение ( например, "Критический успех\!" или "Критический провал\!"). Используйте для этого **дополнительный `if`** внутри блока для d20. * **Сразу несколько кубиков:** * В реальных DND играх может быть необходимым кинуть одновременно сразу несколько разных кубиков. Реализуйте этот функционал